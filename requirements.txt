torch>=2.0
pytorch-lightning>=2.0
timm
einops
numpy
tensorboard
natsort
tqdm
matplotlib
Pillow
lpips
# Optional: only needed for the M9 RemoteCLIP perceptual loss
# (config.train.aux_loss.perceptual_backbone = 'remoteclip')
# open_clip_torch

